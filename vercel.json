{
  "projects": [
    {
      "rootDirectory": "apps/web",
      "buildCommand": "npx prisma generate --schema=../api/prisma/schema.prisma && npx next build"
    }
  ]
}
// Vercel monorepo config - builds only apps/web