{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate --schema=../api/prisma/schema.prisma && npx next build 2>&1",
  "outputDirectory": ".next",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}
