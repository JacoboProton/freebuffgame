{
  "installCommand": "npm install",
  "buildCommand": "cd apps/web && npm install && npx prisma generate --schema=../api/prisma/schema.prisma && ./node_modules/.bin/next build",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}