{
  "buildCommand": "cd apps/web && npx prisma generate --schema=../api/prisma/schema.prisma && npx next build",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}