{
  "buildCommand": "npx prisma generate --schema=apps/api/prisma/schema.prisma && node_modules/.bin/next build apps/web",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}