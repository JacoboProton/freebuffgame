{
  "installCommand": "npm install",
  "buildCommand": "npx prisma generate --schema=apps/api/prisma/schema.prisma && next build apps/web",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}