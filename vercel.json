{
  "framework": "nextjs",
  "installCommand": "npm install",
  "buildCommand": "npm install --prefix apps/web && cd apps/web && npx prisma generate --schema=../api/prisma/schema.prisma && npx next build",
  "outputDirectory": "apps/web/.next",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}