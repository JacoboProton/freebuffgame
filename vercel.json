{
  "framework": "nextjs",
  "buildCommand": "npx prisma generate --schema=../api/prisma/schema.prisma && next build",
  "outputDirectory": ".next",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next"
    }
  ]
}
